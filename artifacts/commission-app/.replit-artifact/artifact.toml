kind = "web"
previewPath = "/"
title = "Commission Manager"
version = "1.0.0"
id = "artifacts/commission-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21322

[services.development]
run = "pnpm --filter @workspace/commission-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/commission-app", "run", "build" ]
publicDir = "artifacts/commission-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21322"
BASE_PATH = "/"
